{
  "layer": "top",
  "position": "top",
  "height": 30,
  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["clock"],
  "modules-right": [
    "cpu",
    "memory",
    "temperature",
    "battery",
    "network",
    "tray",
    "custom/power"
  ],

  "clock": {
    "format": " {:%a, %d.%m. %H:%M}",
    "tooltip-format": "<tt>{calendar}</tt>",
    "calendar": {
      "mode": "month",
      "mode-mon-col": 3
    }
  },

  "cpu": { "format": " {usage}%" },
  "memory": { "format": " {used} / {total} GB" },
  "temperature": { "critical-threshold": 75, "format": " {temperatureC}°C" },
  "battery": { "format": "{capacity}% {icon}", "format-icons": ["", "", "", "", ""] },
  "network": { "format-wifi": "  {essid}", "format-ethernet": "󰈀 {ifname}" },

  "custom/power": {
    "format": "⏻",
    "tooltip": "Herunterfahren / Neustart",
    "on-click": "wofi --show power"
  }
}
